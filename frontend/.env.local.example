# Copy to .env.local (gitignored). Server-only — deliberately NOT prefixed
# with NEXT_PUBLIC_, since the whole point of the BFF pattern (README "前端
# 架构") is that the browser never learns where the api service lives.
API_BASE_URL=http://localhost:8000
